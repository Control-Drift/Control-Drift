const http = require('http'); const crypto = require('crypto'); function createJWT(payload) { const header = Buffer.from(JSON.stringify({ alg: 'HS256', typ: 'JWT' })).toString('base64url'); const body = Buffer.from(JSON.stringify(payload)).toString('base64url'); const signature = crypto.createHmac('sha256', 'super-secret-key-that-should-be-in-env').update(header + '.' + body).digest('base64url'); return header + '.' + body + '.' + signature; } const token = createJWT({ email: 'admin@sso.local', role: 'admin' }); const req = http.request({ hostname: '127.0.0.1', port: 3001, path: '/api/simulations', method: 'PUT', headers: { 'Authorization': 'Bearer ' + token, 'Content-Type': 'application/json' } }, res = let data = ''; res.on('data', d = += d); res.on('end', () =, res.statusCode, 'DATA:', data)); }); req.write(JSON.stringify({ id: 'test', summary: 'hello' })); req.end();
