## 2026-06-16T18:06:00-04:00
You are the teamwork_preview_worker for the stress testing and metrics validation audit project.
Your working directory is: C:/Users/thoma/.gemini/antigravity/scratch/eclipse-ops/.agents/worker_stress_m1_1/
You must perform Milestone 1 (Data Generation & Injection) and Milestone 2 (Metrics Verification & Programmatic Verification).

1. Generate a massive, randomized synthetic dataset:
   - At least 10,000 executed simulations (exercises) containing a randomized mix of high (Optimal), medium (Partial), minimal, low (No Coverage), error, and pending statuses.
   - Exercises must be distributed across various environments (e.g. "Linux", "Windows Server", "Windows Workstation", "macOS", "Azure / Entra ID").
   - Inject several multi-TTP gaps (at least 1,000 gaps) with Critical, High, Medium, Low severities and various statuses (Open, In Progress, Resolved, Risk Accepted).
   - Stagger creation and resolution dates for resolved gaps to simulate complex MTTR metrics. Stagger some resolved gaps to have out-of-sync dates (resolvedDate before createdDate) to verify negative bounding. Stagger some resolved gaps to have invalid dates.
   - Write this generated dataset to the JSON file at `C:/Users/thoma/.gemini/antigravity/scratch/eclipse-ops/synthetic_stress_data.json`.
   
2. Programmatically verify the metrics logic:
   - Review how GRS, MTTR, and Heatmap rollups are calculated in the codebase (refer to `C:/Users/thoma/.gemini/antigravity/scratch/eclipse-ops/src/components/Dashboard.jsx`, `C:/Users/thoma/.gemini/antigravity/scratch/eclipse-ops/src/AppContext.jsx`, `C:/Users/thoma/.gemini/antigravity/scratch/eclipse-ops/mock_database.js`).
   - Write a verification script `C:/Users/thoma/.gemini/antigravity/scratch/eclipse-ops/verify_metrics_stress.js` to mathematically check calculations on the 10,000+ dataset.
   - Run the script and capture the output to verify:
     - The global MITRE Heatmap correctly displays the "Average Coverage" of the TTPs rather than defaulting to the weakest link.
     - `error` and `pending` statuses are completely ignored in the denominator of the coverage calculations.
     - The Global Risk Score (GRS) accurately calculates against the 10,000+ records.
     - The Mean Time To Remediation (MTTR) successfully bounds negative time intervals generated by out-of-sync dates.
   
3. Verify that the build and tests pass.
4. Output your progress and handoff report in your working directory.
MANDATORY INTEGRITY WARNING:
DO NOT CHEAT. All implementations must be genuine. DO NOT hardcode test results, create dummy/facade implementations, or circumvent the intended task. A Forensic Auditor will independently verify your work. Integrity violations WILL be detected and your work WILL be rejected.
